'use client';

// Re-export existing create group page with new layout
import CreateGroupPage from '@/app/dashboard/searcher/groups/create/page';

export default function SearcherCreateGroupPage() {
  return <CreateGroupPage />;
}
