'use client';

// Re-export the existing applications page with the new layout
import MyApplicationsPage from '@/app/dashboard/searcher/my-applications/page';

export default function SearcherApplicationsPage() {
  return <MyApplicationsPage />;
}
