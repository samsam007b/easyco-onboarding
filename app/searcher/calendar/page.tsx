'use client';

// Re-export existing calendar page with new layout
import CalendarPage from '@/app/dashboard/searcher/calendar/page';

export default function SearcherCalendarPage() {
  return <CalendarPage />;
}
